/* const express = require("express");
import {
    create,
    read,
    list,
    update,
    remove
  } from "../controllers/place";
const router = express.Router();

router.post("/create-place", create)
router.get("/read/:id", read)
router.get("/list-places", list)
router.put("/update-place/:id", update)
router.delete("/delete-place/:id", remove)


module.exports = router; */